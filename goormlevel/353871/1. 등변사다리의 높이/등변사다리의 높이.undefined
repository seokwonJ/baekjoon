using System;
namespace goorm
{
	class Program
	{
		static void Main(string[] args)
		{
			string input;
			input = Console.ReadLine();
			string[] s = input.Split(" ");

			int height = (int.Parse(s[0]) * int.Parse(s[0]) * 4 - ((int.Parse(s[2]) - int.Parse(s[1]))) * ((int.Parse(s[2]) - int.Parse(s[1]))));
			
			Console.WriteLine(height);
		}
	}
}